import styled from 'styled-components/native';

const MetaInfoRow = styled.View`
  flex-direction: row;
  align-items: center;
`;

const MetaInfoText = styled.Text``;

export { MetaInfoRow, MetaInfoText };
